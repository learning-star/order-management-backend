# 服务器配置文件

# 数据库配置
[database]
db.host=localhost
db.port=3306
db.user=asta
db.password=Lidaxing666
db.name=PROD_DB

# AP服务配置
[ap]
port = 8081
max_connections = 100

# DISP服务配置
[disp]
port = 8080
max_connections = 100

# AP端点配置
[ap.endpoints]
user = http://localhost:8081
order = http://localhost:8081
product = http://localhost:8081

# 日志配置
[logging]
level = INFO
file = logs/server.log